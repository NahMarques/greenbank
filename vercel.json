{
  "version": 2,
  "installCommand": "npm cache clean --force && npm install --legacy-peer-deps",
  "buildCommand": "npm run build",
  "outputDirectory": "dist/greenbank",
  "framework": "angular",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

